import {
    ImageBackground,
    Text,
    View,
    Image,
    TouchableOpacity,
    Animated,
  } from "react-native";
import React, { useEffect, useRef } from "react";
import { useNavigation } from "@react-navigation/native";

const LandingScreen = () => {
    return (
        <View classname = "w-100%, h-100%, bg-[#263242]">

        </View>
    );
};